# Changelog

All notable changes to this lab will be documented here.
New content is **added**, never destructively replaced.

## 2026-08-20 (repo-wide verification pass)
- Corrected the setup path in the README and notebooks (`cd core-concepts/data-modeling` → `01-foundations/data-modeling`).
- Normalised the notebook kernel to the lab's own `.venv`.
- All 5 notebooks executed end-to-end against the docker-compose stack to verify they run cleanly.
