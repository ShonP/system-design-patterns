[project]
name = "long-running-tasks"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "redis>=5.0.0",
    "psycopg2-binary>=2.9.9",
    "ipykernel>=6.29.0",
]

[dependency-groups]
dev = [
    "nbclient>=0.10.4",
]
