[project]
name = "elasticsearch-lab"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "elasticsearch>=8.13.0,<9",  # must match the 8.13 server in docker-compose.yml
    "jupyter>=1.0.0",
    "ipykernel>=6.28.0",
]
